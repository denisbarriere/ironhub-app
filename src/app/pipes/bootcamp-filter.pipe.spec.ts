import { BootcampFilterPipe } from './bootcamp-filter.pipe';

describe('BootcampFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BootcampFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
