import { CampusFilterPipe } from './campus-filter.pipe';

describe('CampusFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CampusFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
